@inherits Umbraco.Web.Mvc.UmbracoViewPage<BaseModel>
<div class="breadcrumbs">
    <ul>
        @foreach (var ancestor in Model.Content.Ancestors().Where(t => t.IsVisible()).OrderBy(x=>x.Level))
        {
            <li><a href="@ancestor.Url">@ancestor.Name</a></li>
        }
    </ul>
</div>